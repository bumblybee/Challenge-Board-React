import styled from "styled-components";
import { Link } from "react-router-dom";

const StyledNavHeader = styled(Link)`

  color: var(--title-text);
  font-family: "Roboto Slab", serif;
  font-weight: 500;
font-size: 1.1rem;
`;


export { StyledNavHeader };
